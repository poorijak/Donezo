import Landing from "@/feature/landing/landing";
import React from "react";

const page = async () => {
  return (
    <div className="h-screen">
      <Landing />
    </div>
  );
};

export default page;
